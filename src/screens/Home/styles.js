import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    parent: {
        flex: 1,
        width: '100%',
        height: '100%',
        backgroundColor: '#fff',
        flexDirection: 'column',
    },
});